const circuitComponents = [];
class CircuitBoard {

    //array of the components


    //initiate the circuitComponents array with empty components
    constructor(numberOfComponents) {
        this.circuitComponents = [];
    }

    //checks that all the circuit components are not EMPTY and
    //that all switches are closed

    isCircuitClosed() { };

    //returns float
    getCircuitCurrent() { };
    getComponents() { };

}